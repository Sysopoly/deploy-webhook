name: Build and Deploy

on:
    push:
        branches: [master]
    workflow_dispatch:

jobs:
    build:
        uses: Sysopoly/github-actions/.github/workflows/build-and-push.yml@master
        with:
            image-name: sysopoly-deploy-webhook
            run-tests: true
            go-version: "1.25"
        permissions:
            contents: read
            packages: write
